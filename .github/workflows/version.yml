on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

name: CICD on versioning

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: run deploy
        run: |
          echo "run deploy"
